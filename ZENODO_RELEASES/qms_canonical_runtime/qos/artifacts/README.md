# Artifacts

Runtime outputs, figures, generated arrays, videos, papers, release bundles,
and archival manifests belong outside source modules.

Source code must not import from artifacts. Reproducible runs should emit
artifact manifests that describe produced files, parameters, and source commit
or snapshot lineage.

