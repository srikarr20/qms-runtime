# Evolving Benchmarks

Evolving benchmarks preserve version lineage while a protocol is still being
developed. When a version is declared frozen, copy the exact protocol into
`qos/benchmarks/frozen/<benchmark-id>` and stop modifying it.

