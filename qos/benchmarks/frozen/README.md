# Frozen Benchmarks

Frozen benchmarks are archival protocol references. They allow errata,
manifests, and reproducibility wrappers, but no semantic changes to benchmark
logic, scope, pass/fail criteria, or interpretation rules.

